{% extends 'base.html.twig' %}

{% block body %}
    <ul>
        {% for post in posts %}
            <div class="mb-3">
                <h3>{{ post.title }}</h3>
                <p>{{ post.content }}</p>
            </div>
        {% endfor %}
    </ul>
{% endblock %}